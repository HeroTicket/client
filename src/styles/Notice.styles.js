import styled from 'styled-components';

export const NoticeModalTitleContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  white-space: pre-line;
  border-bottom: 1px solid #999;

  h3 {
    font-size: 1.5rem;
  }

  > p {
    font-size: 0.8rem;
    color: #999;
  }
`;
